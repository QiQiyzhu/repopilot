# Verification record

The original [validation JSON](evidence/validation.json) is generated from actual pytest JUnit and Playwright reports. The DeepSeek integration suite has **93 passing cases and one explicitly skipped Windows symlink privilege test**; [record](evidence/deepseek-validation.json) and [raw JUnit](evidence/deepseek-backend-junit.xml). Ruff and mypy pass across 22 backend source files. The earlier [decision-case record](evidence/decision-validation.json) retains its historical 59/1 counts. The latest browser integration exposes the configured server model, blocks unconfigured remote submission and sends explicit finite budgets. Four unit tests, typecheck, lint and build pass. **Seven browser checks** pass locally: six actual-backend workflows and one intercepted DeepSeek request contract; this additional check makes no paid request. Each push CI repeats the backend, browser and container checks.

An actual [DeepSeek integration probe](evidence/deepseek-smoke.json) passed its JSON/nonce contract with one HTTP response, 107 input tokens, 47 output tokens and measured latency of 1047 ms. It sent no repository data and executed no tools. Source hashes bind the run to the in-progress implementation. Injected HTTP tests separately cover malformed/refused/truncated responses, endpoint restrictions, usage validity, timeout and redaction; they are not counted as actual model calls. The real 36-task ablation remains unrun.

The [actual DeepSeek authored-fixture run](evidence/deepseek-task.json) was accepted after four model calls: failing reproduction, model-generated patch, passing retest, then finish. The verifier independently reran three pytest checks. Original tests stayed unchanged and only `calculator.py` changed. Recorded usage is 10,277 input / 323 output tokens; harness latency is 5.437 seconds. The public receipt redacts specific local paths and preserves the original receipt hash, source hashes, process failures and successes. This narrow task is not a representative benchmark or an ablation comparison.

The browser suite covers MCP execution, persisted task reload, diff, verifier, context, retry, memory invalidation, evaluation labels, missing-provider failure, mobile layout and approval/rejection/cancellation of a fixture deletion. The video and screenshots are captures of the running local application, not UI mockups. The updated diff preserves CRLF and changes only the repaired comparison.

The pinned ARC baseline independently passes 92 regression tests, typecheck and build. All 36 task contracts pass positive and negative controls; the six add-test reference cases also reject their intended mutation. The five-profile deterministic harness ablation uses one known fixture per profile and must not be read as an LLM success-rate comparison.

The [new completion counterexample](decision-case-study.md) actually runs two authored candidate tests through the existing verifier: both pass **93** regression tests, yet only the diagonal reference rejects the known distance mutation. Source hashes, process results, exact patches and restored-implementation evidence are retained in [decision-case.json](../evaluation/results/decision-case.json). No provider is called. A real timed-out subprocess also verifies that infrastructure timeout cannot count as a successful negative control. After tightening the shared rejection predicate, all **36** contract controls and **6** mutation controls were rerun and passed. The optional paid-model runner uses the same predicate but was not invoked.

Dependency audit on the frontend reports zero known vulnerabilities at the recorded check. Vitest was moved to 4.1.11 after inspection of the [official advisory](https://github.com/advisories/GHSA-82fw-gwwq-j7x9). This does not guarantee that all dependencies are vulnerability-free.

Local runtime: Python 3.12.14, Node 24.16.0, Windows, Edge browser. The historical [Linux CI run 34445670524](https://github.com/QiQiyzhu/repopilot/actions/runs/34445670524) passed **58 backend tests** (including the symlink case), the frontend checks and **6 real-service Chromium workflows**, plus the container runtime job. Docker is not installed on the Windows development host. The newer remote-model probe is separately recorded above; there is no production-usage claim.

The [container runtime acceptance](container-runtime.md) starts the built Nginx and FastAPI services, waits for startup health and sends requests through **8080**. It fetches actual frontend HTML/JavaScript, submits a known MCP fixture task, observes failed reproduction then successful repair, and requires an independent pytest verifier (**3 passed**) plus an exact diff. After restarting the backend, the same task, verification and diff remain available; SSE also passes through the proxy. Original JSON, patch and Compose logs are retained in [container evidence](evidence/container-runtime/). Both service containers, the CI network and the disposable named volume were cleaned up. This is packaged-system validation, not real LLM performance or production-load testing.
